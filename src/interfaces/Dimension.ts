export interface IDimension {
  width: number
  height: number
  halfWidth: number
  halfHeight: number
  json: { width: number, height: number }
}

export interface IDimensionProps {
  width: number
  height: number
}